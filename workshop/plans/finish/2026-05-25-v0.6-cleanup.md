# v0.6 Cleanup Cut — Implementation Plan (archived)

> **Archived plan** — shipped in commit `e9e0c85` (v0.6.0). Kept as historical record. Step-by-step Edit prescriptions used during execution are not preserved; this is a summary of what each task accomplished.

**Goal**: Internal consistency cleanup. Remove duplicated doctrine between `session-exit/SKILL.md` and `exit-ritual.md`, harden frontmatter enforcement to fail-loud, encode the lifecycle as a Mermaid diagram, document plan-checkbox non-binding-ness, and add wip TTL hard gate.

## Tasks shipped

### Task 0 — Session entry preflight

Read `workshop/board.md`, confirm Active focus matches the plan, verify clean working tree, spot-check the v0.6 section of the roadmap spec hadn't drifted.

### Task 1 — De-duplicate `session-exit/SKILL.md` against `exit-ritual.md`

`skills/session-exit/SKILL.md` rewritten from 113 → 53 lines. Stripped duplicated lifecycle transitions table, (a)–(h) classification heuristics, `Last updated` triggers, and the "Don't do" list. SSOT for exit-ritual operations stays in `exit-ritual.md`. Anchor links into referenced sections verified.

### Task 2 — Hard-fail on missing scars/playbooks frontmatter

`skills/workshop-workflow/SKILL.md` got a new `### Frontmatter requirements (hard-fail)` section + a sharpened Common Mistakes row: missing `when_to_read` / `applies_to` / `last_updated` now triggers STOP-and-report instead of silent-skip. `templates.md` scar + playbook rules bumped from advisory to REQUIRED.

### Task 3 — Document plan-checkbox non-binding-ness

`skills/workshop-workflow/SKILL.md` Cross-references section gained a paragraph noting that plan files may use `- [ ]` checkboxes for execution tracking, but workshop does not require flipping them — progress lives in `board.md` + commit log.

### Task 4 — Add Mermaid lifecycle state diagram

`stateDiagram-v2` block inserted into `skills/workshop-workflow/SKILL.md` between the Lifecycle preamble and the Implicit states list. 16 transitions + 6 `note right of` entries with emoji legends.

### Task 5 — wip TTL frontmatter + session-enter surfacing + session-exit blocking

Added new `## wip` template to `templates.md` with `last_touched:` required field. `session-enter/SKILL.md` gained a stale-wip surfacing step. `exit-ritual.md` "Stale wip" section rewritten from advisory "default to deletion" to hard-gate "session-exit BLOCKS until classify / delete / defer with `defer_reason:`".

### Task 6 — Release v0.6.0

VERSION bumped 0.5.0 → 0.6.0. CHANGELOG entry added. board.md updated. Tag `v0.6.0` created. Plus a fix-up commit that caught a missed plugin-manifest version bump and a folder-semantics wip note.

## Outcome

v0.6.0 shipped clean. The cycle revealed that the release task hadn't included plugin-manifest version verification — that gap motivated the capability × tool matrix work in v0.7.
